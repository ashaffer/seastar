namespace seastar {
	// namespace program_options {
	// 		class config {

	// 		    // app.add_options()
	//             //         ("port", bpo::value<uint16_t>()->default_value(10000), "RPC server port")
	//             //         ("server", bpo::value<std::string>(), "Server address")
	//             //         ("compress", bpo::value<bool>()->default_value(false), "Compress RPC traffic");

	// 		class options {
	// 		public:
	// 			template<class T>
	// 			options& operator() (const char *name, value<T>&& t, const char *desc = nullptr) { 
	// 				typed_value<T> *v{new typed_value<T>} *v{typed_value<T>;
	// 				return *this;
	// 			}

	// 		private:
	// 			std::map<std::string, value, std::string>
	// 		};

	// 		class value {
	// 		public:

	// 		private:

	// 		};
	// 		template<class T>
	// 		class typed_value {
	// 		public:
	// 			typed_value *default_value (T&& t) noexcept {
	// 				def_val{std::forward<T>(t)};
	// 				return this;
	// 			}
	// 		private:
	// 			std::optional<T> def_val;
	// 		};
	// 	};
	// };
};